"""Third-party integrations (Azure Blob, Meta WhatsApp, Google Calendar, SMTP).

Modules here wrap external SDKs so the rest of the codebase imports a small,
testable surface (and so tests can monkeypatch a single seam per integration).
"""
